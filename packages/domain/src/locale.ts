/**
 * The product is single-market: an Indian tourist belt. These are not defaults to
 * be overridden per user — they are the locale the whole system operates in, and
 * every formatter in every app derives from them.
 */

export const TIMEZONE = "Asia/Kolkata";

export const LOCALE = "en-IN";

export const CURRENCY = "INR";
